

class Animal {

    private name: string;// prpeties

    constructor(theName :string){ // method
     this.name = theName ; }
     
     move (meter : number){

         alert(this.name + " moved " + meters + "m.")
    }
}

var a = new Animal("dog").neme;
console.log(a);